# pageBideApp

Main entry point for the application.

This component is responsible for handling internal navigation. Routes are defined internally using custom logic based on `window.location.pathname`.
