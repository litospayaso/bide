# pageCamera

This component provides a live camera feed using HTML5 `getUserMedia`. It allows users to overlay a reference image on top of the camera feed (useful for tracing/drawing), adjust its transparency, and export the combined view as a PNG screenshot.
